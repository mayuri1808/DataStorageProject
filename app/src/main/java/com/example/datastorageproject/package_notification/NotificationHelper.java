package com.example.datastorageproject.package_notification;

import android.content.Context;

import androidx.core.app.NotificationCompat;
import androidx.core.app.NotificationManagerCompat;
import com.example.datastorageproject.R;

//This class is for displaying notification
public class NotificationHelper {

   /* public static void displayNotification(Class<? extends MyFirebaseService> context, String title, String body){
        NotificationCompat.Builder builder=new NotificationCompat.Builder
                (context,Activity_SimpleNotification.CHANNEL_ID)
                .setSmallIcon(R.drawable.ic_sd_card_black_24dp)
                .setContentTitle(title)
                .setContentText(body)
                .setPriority(NotificationCompat.PRIORITY_DEFAULT);

        //To display or to build notification
        NotificationManagerCompat notificationManagerCompat=NotificationManagerCompat.from(context);
        notificationManagerCompat.notify(1,builder.build());
    }*/

}
